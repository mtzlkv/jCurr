# jCurrency

Currency converter app (Expo) with Feature-Sliced Design.

## Setup

```bash
npm install
npx expo start
```

## Structure

See [src/README.md](src/README.md) for FSD layout. Uses ExchangeRate-API (fiat) and CoinGecko (crypto).
